# minder-docs

Published documentation for **Minder**, a self-hosted home-security appliance.

**This repository is generated.** Every file here is built from the private `minder` repo by
`scripts/publish_docs.sh` and overwritten on each publish — edits made here are lost. Fix the
source instead.

Served at <https://delivstat.github.io/minder-docs/>.
