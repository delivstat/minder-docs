{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Minder","text":"<p>A self-hosted home-security appliance. It watches your cameras, answers questions about them in plain language, controls your devices, and \u2014 the part most systems skip \u2014 tells you when it could not see.</p> <p>Everything runs on one machine in your house. No footage leaves it unless you deliberately turn that on.</p>"},{"location":"#what-it-does","title":"What it does","text":"<p>Watches. Frigate detects; Minder decides whether a detection is worth an alert, and sends one with a snapshot and a description of what is actually in frame \u2014 not \"motion detected\", but what the camera is looking at.</p> <p>Answers. \"Is the gate open?\" \u00b7 \"Anything at the door today?\" \u00b7 \"Was anyone unrecognised at the gate last night?\" Asked from Telegram or Discord, answered from your own cameras.</p> <p>Acts. Lights, switches and pumps through Home Assistant. Standing rules in plain language: \"turn on the pump when there is a person at the main door\".</p> <p>Admits what it missed. A camera that was offline between 02:00 and 02:40 is recorded as a gap. So \"nobody was there\" and \"nobody was watching\" are different answers, and you are told which one you got.</p> <p>Recognises, optionally. Face plugins can put a name to people you enrol \u2014 and flag anyone they do not recognise. A match only ever adds a name to an alert; it can never silence one.</p>"},{"location":"#the-idea-it-is-built-on","title":"The idea it is built on","text":"<p>A small local model does exactly one job: turn your sentence into a structured plan. It does not act, and it does not call tools in a loop. Deterministic code executes the plan.</p> <pre><code>\"is someone at the front door\"\n   \u2192 router: {kind: query, camera: Front-Door}\n   \u2192 code:  fetch snapshot \u2192 describe \u2192 reply\n</code></pre> <p>An agent that both decides and acts fails in ways you cannot reproduce. Splitting them means the only thing that varies is the parse \u2014 and the parse is measurable. On a 166-case benchmark the router reads intent correctly 90% of the time, on a 3B model running on the appliance itself.</p> <p>How it works \u2192</p>"},{"location":"#what-it-runs-on","title":"What it runs on","text":"<p>An Intel N100 mini-PC handles eight cameras. There is no GPU requirement \u2014 the vision model runs on CPU deliberately, because on this hardware a GPU thrashes between the detector and the model and destabilises detection, which is the thing the appliance is actually for.</p> cameras 8 on a reference N100 language model ~2GB, local cloud optional, per-feature, off by default footage leaving the house none unless you enable it"},{"location":"#start-here","title":"Start here","text":"<ul> <li>Walkthrough \u2014 six short clips of the portal, recorded against a demo instance</li> <li>Using Minder \u2014 install it, connect cameras, talk to it, write rules</li> <li>Faces \u2014 what is stored, groups, and proof that deletion worked</li> <li>How it works \u2014 containers, the router split, plugins, state</li> <li>Detector tuning \u2014 how the detector and face models were measured, and why the stream's resolution mattered more than the model</li> <li>Privacy \u2014 what is kept, what leaves, and what you can delete</li> </ul>"},{"location":"architecture/","title":"How Minder works","text":"<p>The technical design of Minder as it runs today: every core subsystem and every feature, each as a decision and the reason for it. For using Minder, see Using Minder. For the argument behind any one decision at full length \u2014 including the options that lost \u2014 the <code>design/</code> directory has 24 notes, and <code>design/README.md</code> is the audited index of what each one decided and whether it shipped.</p> <p>The reasons are written down because on an appliance they are load-bearing. Several decisions here were reversed after a measurement contradicted what looked obviously true; each reversal is noted where it happened and collected at the end. Where a number is quoted it was measured on the reference machine \u2014 an i7-8750H with a 4 GB GTX 1050 Ti and seven cameras \u2014 with a script that is in the repository and can be re-run.</p>"},{"location":"architecture/#1-what-minder-is","title":"1. What Minder is","text":"<p>A home security appliance that watches cameras, tells you in plain words what it saw, and does what you ask it in plain words \u2014 running entirely on one small computer in the house. No frame leaves the building unless you turn a cloud tier on.</p> <p>Four properties follow from \"appliance\", and most decisions below trace back to one of them:</p> <ol> <li>It must keep working when everything else fails \u2014 the language model down, the network down,    an accelerator driver missing, a channel blocked. Detection is the floor; everything else degrades    above it and says so.</li> <li>It runs on a 4 GB card and a laptop-class CPU. Every model choice is a residency question    before it is a quality question.</li> <li>Nobody is watching the logs. A silent failure is worse than a loud one; a component that is    quietly not doing its job must show on the dashboard in words.</li> <li>It is talked to, not configured. A household says \"alert me if someone is at the gate after    dark\"; that sentence must become a running rule, and the household must be able to read the rule    back.</li> </ol>"},{"location":"architecture/#2-the-shape","title":"2. The shape","text":"<p>Five containers on one host, all on the host network, plus one host process:</p> Container What it does <code>frigate</code> Camera detection \u2014 RTSP in, tracked objects out over MQTT, recordings and snapshots <code>mosquitto</code> The MQTT bus: Frigate's events in, Minder's alerts out <code>homeassistant</code> Device control (switches, lights, sensors) and weather \u2014 a hidden integration layer <code>minder</code> Everything that is Minder: the swarm, the ops API and dashboard, the channel adapters <code>frigate-init</code> One-shot: writes Frigate's configuration from the camera list, then exits <p>Ollama is a host process, not a container. The models are reached over <code>localhost:11434</code>. That keeps camera frames on the host and lets the GPU be shared by exactly one thing; it also means a fresh machine needs one model pulled before the stack is useful \u2014 <code>gemma3:4b</code>, which is the router, the chat and the VLM. There is no tool-calling model: setup, discovery, device control and the doctor are code, and work with Ollama not running. <code>MODELS.md</code> lists every model, which property sets it and the benchmark behind each choice, and is generated from the repository because an inventory assembled by hand from five places is the document most certain to rot.</p> <p>The <code>minder</code> container supervises four processes, each restarted independently five seconds after a crash \u2014 a dead Telegram bot does not take the cameras with it:</p> <pre><code>swarmkit serve /app/workspace --port 8321   # the agent swarm, HTTP on localhost only\npython3 /app/webapp/app.py                  # onboarding, dashboard, ops API \u2014 port 80\npython3 /app/bot.py                         # Telegram adapter\npython3 /app/discord_bot.py                 # Discord adapter\n</code></pre> <p>The swarm runs on SwarmKit, pinned once as <code>SWARMKIT_VERSION</code> in <code>docker-compose.yml</code>, consumed from PyPI. It runs no tools: the model turns language into a plan and describes what it sees; the capability modules (<code>mcp-servers/camera</code>, <code>tuya</code> \u2014 Home Assistant \u2014, <code>frigate</code>, <code>detect</code>) are plain functions the webapp calls in-process. They were once wired to the runtime as MCP servers for tool-calling setup topologies, which meant a second LLM had to be pulled and running before a single camera could be found; that model only ever relayed the function's result, and is gone.</p>"},{"location":"architecture/#3-the-principles","title":"3. The principles","text":"<p>Five rules recur in every subsystem. They are stated once here so the sections can refer to them.</p> <p>The model reads, the code does. A language model turns a sentence into a structured plan and stops; deterministic Python executes the plan. Never an agent that decides and acts in a loop \u2014 that fails in ways you cannot reproduce. The only thing that varies is the parse, and the parse is testable.</p> <p>Everything is data. Rules, zones, plugins, backends, models, the platform \u2014 YAML or JSON files you can read, list and delete. No feature generates code.</p> <p>Local first; cloud is a tier you turn on. Every path has a fully local form. A cloud tier improves quality and, when it fails, falls back to local: a cloud outage degrades quality, never availability.</p> <p>Fail open on infrastructure, closed on evidence. When Minder cannot look (no frame, a model down), the safe behaviour is the old behaviour. When it looked and saw nothing, the safe behaviour is to say nothing \u2014 and record that it did.</p> <p>Measure, then decide; and say so when it reverses. Every model, prompt and residency choice in this document has a script behind it. The ones that were reversed are listed in \u00a714 with the number that reversed them.</p>"},{"location":"architecture/#4-conversation-from-a-sentence-to-an-action","title":"4. Conversation: from a sentence to an action","text":"<p><code>design/conversation-router.md</code>, <code>design/alert-quality.md</code> \u00a7part 3.</p> <p>Every message from every channel arrives at one function, <code>handle_message</code>, and goes through four stages:</p> <pre><code>\"is someone at the main door\"\n   1. feedback?      no\n   2. pre-route      no pattern matches \u2192 the model decides\n   3. router         gemma3:4b \u2192 {kind: query, subject: person, cameras: [Front-Door]}\n                     _correct_plan \u2192 (unchanged)\n   4. dispatch       code: grab frame \u2192 YOLO \u2192 \"\ud83d\udd34 Yes \u2014 person at Front-Door.\"\n</code></pre>"},{"location":"architecture/#41-the-router-is-one-structured-call","title":"4.1 The router is one structured call","text":"<p><code>minder-router</code> is a SwarmKit topology whose root agent has an <code>output_schema</code> and no tools. It sees the camera and device names of this house prepended to the message, and emits one JSON plan: <code>kind</code> (one of ten intents), and the fields that intent needs \u2014 camera, subject, device, operation, trigger, schedule. Ollama constrains decoding to the schema, so the shape is guaranteed; the prompt carries the meaning of each intent, because a grammar drops the field descriptions and a small model needs them (measured: skipping the pasted schema cut one intent from 55% to 29%).</p> <p>Why one call and not an agent with tools. Small models loop on list-tools, invent camera names and take different paths on identical input. Giving the router the names in the prompt and no tools removes every one of those failure modes. The plan it emits is a value; values are testable.</p>"},{"location":"architecture/#42-the-guards","title":"4.2 The guards","text":"<p>The plan passes through <code>_correct_plan</code>, a set of deterministic corrections, before anything acts on it. Each guard restates something an intent already means:</p> <ul> <li>A scenario is a standing rule with a trigger \u2014 so \"turn on the pump\", with no trigger, is a   command. This matters beyond accuracy: a scenario is stored, so a command misread as a rule   silently never runs.</li> <li>A device command must say what to do \u2014 so \"what smart plugs do I have\" is a listing, not an   instruction to switch one. Answering a question by switching a device is the wrong kind of wrong   for a house.</li> <li>A message naming one of this house's cameras is about that camera \u2014 the names are <code>Side-Gate</code>,   people type \"side gate\", and matching those is code's job.</li> <li>A check across the cameras is a sweep, not a listing \u2014 \"check all cameras and tell me what you   see\" once returned seven camera names.</li> </ul> <p>The guards are specific to the model deployed and are re-measured whenever it changes: on the original <code>qwen2.5:3b</code> they corrected 55 of 156 answers and were the difference between unusable and usable; on <code>gemma3:4b</code> they correct 16; on <code>qwen3.5:4b</code> they correct 3.</p>"},{"location":"architecture/#43-pre-routing-the-certain-cases-never-reach-a-model","title":"4.3 Pre-routing: the certain cases never reach a model","text":"<p>Before the router runs, <code>router_learn.preroute</code> decides the shapes code can be certain of: a greeting, an on/off order naming one device with no trigger or time word, a device or camera listing, a weather question. 45 of the 166 labelled cases; held to 100% precision by a test that runs every labelled case through it \u2014 a pattern that ever fires wrongly on a known message fails the build.</p> <p>Two deliberate refusals define the boundary. \"turn off the pump at midnight\" is not pre-routed: a time word makes it a scenario, and pre-routing it would switch the pump off now. \"is the pump on\" is not pre-routed: a question is never an order, however much it looks like one.</p> <p>Measured: guarded accuracy unchanged, raw accuracy up three points, the median route 10% faster, and a benchmark run a quarter shorter.</p>"},{"location":"architecture/#44-the-case-set-is-the-specification","title":"4.4 The case set is the specification","text":"<p><code>scripts/router_cases.json</code> holds 166 single-intent utterances (plus ten compound ones, unscored) drawn from this house's own phrasings, each labelled with its intent. Every change to the router \u2014 prompt, model, guard \u2014 is measured against it, two ways:</p> <ul> <li><code>scripts/bench_router_models.py</code> calls Ollama directly with exactly the prompt the topology sends   (it imports the same context builder), so candidates can be compared without restarting a live   appliance. Flags reproduce every variable that ever mattered: <code>#ctx</code> context size, <code>+gpu</code>   residency, <code>+paste</code> the compiler's schema-in-prompt, <code>+preroute</code>, <code>+fewshot</code>.</li> <li><code>scripts/bench_router.py</code> runs each case through the real topology on the running server \u2014   schema, guards and all. It is the number that means something absolutely; the direct harness with   <code>+paste</code> reproduces it to the decimal and is used to pick.</li> </ul> <p>Both run under <code>scripts/bench_isolated.sh</code>, which stops every container but Ollama first and snapshots the router's context: a number taken while Frigate, Home Assistant and the VLM share the card is a number about that afternoon.</p> <p>The set grows from the phone. Replying <code>wrong</code> (or <code>wrong: query</code>) to a Minder answer stages the last routed message as a case with its expected intent; <code>scripts/router_feedback.py --merge</code> brings the labelled ones into the repository under review, tagged <code>feedback</code>. The place a misroute is noticed and the place the router's spec lives finally meet. <code>scripts/harvest_router_cases.py</code> does the same from the observation log in bulk.</p>"},{"location":"architecture/#45-which-model-routes-and-why","title":"4.5 Which model routes, and why","text":"<p>Measured through the real topology, in isolation, after the prompt fixes of the same change:</p> model raw guarded guard corrections p50 resident with the VLM? <code>gemma3:4b</code> (default) 86.1% 94.0% 16 2.1 s yes \u2014 it is the VLM <code>qwen3.5:4b</code> 95.8% 96.4% 3 5.1 s no \u2014 evicts it <code>qwen2.5:3b</code> (previous) 60.3% 92.3% 55 2.1 s no <p><code>gemma3:4b</code> routes because it is already resident for vision. A 4 GB card holds one 4B model; a router on a different model evicts the VLM on every message and the next alert pays a cold load \u2014 that eviction was the bulk of a ~27 s round trip. One model, one runner, no reload. The 2.4 points <code>qwen3.5</code> would add cost 2.4\u00d7 the latency and the residency; <code>.env.example</code> says how to choose it.</p> <p>Three things were learned measuring this, each of which reversed an assumption:</p> <ul> <li>The biggest lever was the prompt, not the model. A <code>device_list</code> line with examples, the   conditional tense for scenarios (\"if / when / let me know if / watch\"), device status questions   named as <code>device_now</code>, and \"check the house\" as a query moved every candidate by 2\u20139 points. The   model swap alone, before those fixes, moved nothing.</li> <li>Few-shot retrieval made it worse. The five nearest labelled cases as examples cost gemma3 six   points raw: the phrasings closest to a message are its confusable ones, and the model copies the   top example. Restricting to unanimous neighbours still did not beat the plain prompt. It is not in   the live path; the harness keeps the flag for the next model.</li> <li>Residency is not about context size. llama.cpp's auto-fit keeps ~1.7 GB of the card free for a   multimodal model's worst-case projector, so a 4B VLM without <code>num_gpu: 99</code> runs half on the CPU   whatever <code>num_ctx</code> is (1783 of 3554 MiB in VRAM; p50 5.5 s against 3.7 s pinned). The router pins   <code>num_ctx 4096 / num_gpu 99 / keep_alive -1</code> to match the vision archetypes exactly \u2014 the same model   with a different option is a second runner, and a request at Ollama's default <code>keep_alive</code> quietly   un-pins the runner the vision path pinned.</li> </ul>"},{"location":"architecture/#46-dispatch-is-code","title":"4.6 Dispatch is code","text":"<p>The plan's <code>kind</code> selects a handler in <code>minder_ops.py</code>; none of them calls a language model to act:</p> kind what runs <code>query</code> grab a frame per camera; YOLO for person/vehicle/animal (fast, deterministic); the VLM only for an open question on one named camera. An open question across all cameras is a YOLO sweep \u2014 one line per camera \u2014 never seven VLM calls <code>snapshot</code> / <code>video</code> grab and send <code>device_now</code> <code>control_device</code>: a status question reads state and never writes; an order calls exactly one Home Assistant service on exactly one entity, from a per-domain allowlist <code>scenario</code> <code>author_scenario</code>: the plan's trigger, device, schedule become a rule in <code>/data/rules.json</code>, shown back before it runs <code>weather</code> the Home Assistant weather entity, relayed verbatim \u2014 an agent added only hallucination here <code>camera_list</code> / <code>device_list</code> / <code>setup</code> / <code>chat</code> listings, onboarding, and the one path that is a conversation (<code>minder-core</code>) <p>\"Is the pump on?\" is the case that shaped <code>device_now</code>. It routed correctly, and the handler's text parse saw \"on\" and switched the pump on. A question about a device is now a read, whatever operation the router filled in \u2014 <code>test_device_status.py</code> fails 14 of 19 on the old code.</p>"},{"location":"architecture/#5-perception-from-a-camera-to-an-alert","title":"5. Perception: from a camera to an alert","text":"<p><code>design/vision-architecture.md</code>, <code>design/frigate-mqtt-events.md</code>, <code>design/alert-quality.md</code> \u00a7parts 1\u20132.</p> <pre><code>camera \u2500\u2500RTSP\u2500\u2500\u25ba Frigate (continuous detection, 5 fps on the substream)\n                    \u2502 MQTT frigate/events  (new / update / end)\n                    \u25bc\n            mqtt_listener \u2500\u2500\u25ba _match_and_fire_event \u2500\u2500\u25ba rule? cooldown?\n            (minute poller = reconcile backstop; same state, never double-fires)\n                    \u2502\n                    \u251c\u2500 escalate rule  \u2500\u2500\u25ba VLM yes/no on a hi-res frame \u2500\u2500\u25ba alert or nothing\n                    \u2514\u2500 plain rule     \u2500\u2500\u25ba SECOND OPINION \u2500\u2500\u25ba alert text now\n                                                          \u2514\u2500\u25ba photo, description, clip as they exist\n                    \u25bc\n            alert bus (MQTT minder/alerts, durable per subscriber) \u2500\u2500\u25ba Telegram \u00b7 Discord \u00b7 dashboard\n</code></pre>"},{"location":"architecture/#51-frigate-detects-minder-decides","title":"5.1 Frigate detects; Minder decides","text":"<p>Frigate is the perception sidecar: it decodes streams, runs a detector continuously and tracks objects. Minder never re-implements that. Minder configures it \u2014 <code>frigate-init</code> writes Frigate's whole configuration from the discovered camera list, the zones drawn on the dashboard and the platform profile's detector block \u2014 and consumes its events.</p> <p>Real-time over MQTT, with the poller kept. Alerts used to be up to a minute late because Minder polled <code>/api/events</code> on a cron. Frigate publishes every tracked object to <code>frigate/events</code>; Minder subscribes and reacts the instant one appears. The minute poller stays as the reconcile backstop for anything missed while MQTT was down, and both paths share one <code>_match_and_fire_event</code> and one dedup/cooldown state, so they can never double-fire.</p> <p>Rules match deterministically. A rule names cameras (or \"all\", \"outside\"), a condition whose words map to labels, optionally a zone, and actions. Escalate rules outrank plain ones for the same event so a broad \"person\" rule cannot shadow a per-camera \"is it a weapon?\" gate. Cooldown is per camera and condition (<code>MINDER_ALERT_COOLDOWN</code>, 600 s) and a rule may declare its own cadence.</p>"},{"location":"architecture/#52-the-second-opinion","title":"5.2 The second opinion","text":"<p>Frigate ships with its default CPU detector \u2014 SSDLite MobileNet-v2 at 320\u00d7320 \u2014 running on a 352\u00d7288 substream. On the reference house it produced 111 <code>person</code> events on one camera in 36 hours. Forty of them on a contact sheet were two shapes: a fence post flickering at the right edge of the frame by day, and the dark bulk of a parked car under the porch light by night (one such event lasted 8828 s). Frigate scored both 0.71 \u2014 exactly what real people on that camera score \u2014 so no threshold separates them, and no area filter does either.</p> <p>Minder's own floor detector does. YOLOv8-n through the inference seam, on the very same snapshots, found 0 of 20 false positives to be a person \u2014 and missed some real ones at 352 px, which is why the gate looks at a bigger picture.</p> <p>So before a plain object rule alerts, <code>_verify_event</code> grabs a 640 px frame from the camera's main stream (the grab the escalate path already had) and asks the seam. Nothing there, then nothing on Frigate's own snapshot either \u2014 the moment Frigate saw it, because a fast person can leave between the two \u2014 and the alert is suppressed and recorded: the frames are kept, the health page counts it, <code>scripts/audit_events.py suppressed</code> lays the frames on a sheet. If the gate is ever wrong the evidence is there and the rule can say <code>verify: off</code>. If Minder cannot look \u2014 no frame, the seam failed \u2014 the alert goes out as before. Live cost: ~1.5 s for the grab and ~0.1 s for the look, ahead of a bot that polls every 10 s.</p> <p>The root cause is upstream, and the platform profile can fix it without a GPU. Frigate's OpenVINO detector runs on the CPU \u2014 no render node, no compose override \u2014 and its <code>yolo-generic</code> model type runs the same YOLOv8-n the seam uses, exported at 320 and shipped in the image (<code>/app/models/yolov8n-320.onnx</code>). A profile that says so:</p> <pre><code>frigate:\n  detector: { ov: { type: openvino, device: CPU } }\n  model:\n    model_type: yolo-generic\n    width: 320\n    height: 320\n    input_tensor: nchw\n    input_dtype: float\n    path: /config/model_cache/yolov8n-320.onnx\n    labelmap_path: /labelmap/coco-80.txt\n</code></pre> <p><code>frigate.model</code> is Frigate's top-level <code>model:</code> block (the description is global in Frigate, not per detector). On reconfigure Minder stages the file from the image into Frigate's config volume, and withholds the block if the detector fell back to <code>cpu1</code> \u2014 a YOLO description handed to the TFLite CPU detector is a Frigate that does not start. On the reference box, seven cameras at 5 fps, it costs 17 ms per inference against SSDLite's 28 ms, and in its first minutes boxed the two cars on the porch as cars at 0.82 with no \"person\" on either. How all of this was measured, and the checklist for repeating it on another installation, is Detector tuning.</p> <p>The detector is only half of it; the other half is the stream. Run offline over a day of real <code>person</code> snapshots, YOLOv8-n at 320 found 0 of 38 children cycling past the far gate \u2014 20 px tall in the 352\u00d7288 substream, below what any COCO detector resolves \u2014 where SSDLite had scored them 0.7. So a camera can detect on its main stream instead: <code>detect: {stream: main, width: 1280, height: 720}</code> on the camera record (the \"Detect on\" select on the dashboard camera card, or <code>POST /api/cameras/detect</code>; <code>MINDER_DETECT_STREAM=main</code> makes it the default for every camera). Frigate decodes the full stream in software (~0.3 core per camera on the reference box, no hardware decode on WSL2), scales to 1280\u00d7720, and crops motion regions at that resolution, so the same child is 70 px inside the 320 crop. A side effect pays for most of it: at 1280\u00d7720 the motion gate stops firing on compression noise, and detection on those cameras dropped from a continuous 5 fps to 0.2 fps. It is per camera because most cameras do not need it; the two that watch far approaches do. Both are being measured against the porch's 36-hour record before they become the detected default; the second opinion stays either way.</p>"},{"location":"architecture/#53-the-alert-in-order","title":"5.3 The alert, in order","text":"<p>The text goes out instantly \u2014 the event has a rule, the second opinion agreed, that is enough. Then, in a thread that never blocks the alert path, as each exists: the photo with its box drawn on (the box is the point of the photo \u2014 Frigate 0.17 stores its snapshot clean whatever <code>snapshots.bounding_box</code> says, so Minder draws it: the seam's box on the 640 px verified frame when there is one, Frigate's box on Frigate's snapshot otherwise), the description, and the recorded clip. A live event has no snapshot at fire time and no clip until it ends; waiting for either inline would have dropped them.</p>"},{"location":"architecture/#54-the-description","title":"5.4 The description","text":"<p>The line under the photo is the most-read thing Minder produces, and it was being written from the worst picture Minder has: the 352 px snapshot with a detection box burned into it. It is now written from the verified 640 px frame, and the prompt says where in it the subject is \u2014 \"It is in the lower right of the frame\" \u2014 from the seam's box, so a small model is told where to look rather than left to search.</p> <p>The prompt is grounded and conservative on purpose: anchored to what the detector found and which rule fired, forbidding the fabrication-prone moves (reading plates and signs, inventing entities, guessing counts, parroting the watch condition). <code>scripts/vlm_bench.py</code> showed the open prompt hallucinating on every model tried; this one removes it. Two sentences by default \u2014 what the subject looks like, then what it is doing \u2014 after <code>scripts/bench_describe.py</code> ran both styles over real frames on gemma3 and the second sentence added \"carrying a green plant\" and \"looking towards the right\" without inventing anything the one-sentence form had not.</p> <p>What both styles do invent is a person on a frame with no person, when told one was detected \u2014 the night-car frame came back as \"a person in a dark jacket, moving right\". That is the second opinion's job, and the reason the description never runs on a frame the seam rejected.</p> <p>The prompt lives in code, interleaved with the situation, rather than in the archetype's system message. That is measured, not neglect: moved to a system message the model started reading the timestamp overlay and the on-screen camera name. It honours a system message only when its output is grammar-constrained, which the yes/no confirm path is and free prose is not.</p>"},{"location":"architecture/#55-severity-and-escalation","title":"5.5 Severity and escalation","text":"<p><code>design/scenario-studio-severity-escalation.md</code></p> <p>A rule may carry <code>severity</code> (info / warning / critical) and an <code>escalate</code> block: a question the VLM must answer yes to on the frame \u2014 \"is the person holding a weapon?\" \u2014 before the alert and any actions fire. The deterministic match is only a pre-filter; the VLM is the gate. Critical rules use the cloud tier automatically, everything else local, cloud failing back to local. The verdict is a schema-constrained field, never the first word of prose \u2014 an off-shape reply is unverifiable, not a no \u2014 and a critical rule that cannot be verified still fires, flagged \"unverified\", because the one failure a security system may not have is a missed critical alert.</p>"},{"location":"architecture/#6-the-vision-models","title":"6. The vision models","text":"<p><code>design/two-layer-vision-models.md</code>, <code>design/vision-via-swarmkit.md</code></p> <p>Two layers, because they have different budgets:</p> <ul> <li>Layer 1 \u2014 per-alert. Runs on every alert, so it must be cheap and local: the description above   and the escalate yes/no, on <code>gemma3:4b</code>.</li> <li>Layer 2 \u2014 interactive. \"What is the person doing?\", \"is that dangerous?\" \u2014 a model that reads   and judges. Local by default; <code>MINDER_QUERY_PROVIDER=openrouter</code> puts a cloud model behind it.</li> </ul> <p>Object presence is YOLO's job, never the VLM's. The VLM describes a snapshot or answers an open question about one. That is the same split Frigate uses, and it is why a 4B model cannot fabricate an intruder into an alert.</p> <p>Every VLM call is a topology. The describe and confirm calls used to be hand-built HTTP requests to Ollama; they now run as <code>vision-describe</code>, <code>vision-confirm</code> and <code>scene-query</code> through SwarmKit. What that buys is not elegance: the model, its options and its provider are workspace configuration, and a call that fires on every alert finally leaves an audit record. Language models are reached only this way \u2014 there is deliberately no second abstraction over them in Minder.</p> <p>On the GPU, and that reversed the original design. The first note said CPU, reasoning that the GPU would thrash between detector and VLM \u2014 sound, and made without a measurement. A real question grabs a fresh frame, so its ~1,100 vision tokens can never hit the prompt cache; every call pays full prompt evaluation, and that is 131.8 s on the CPU against 18.0 s on the GPU. Every \"CPU is fine\" number before it had been taken against a stored snapshot, where the cache hides the entire cost. The rule that came out of it is in <code>CLAUDE.md</code>: measure with a frame the model has not seen.</p> <p>Three options must agree across every vision archetype and the router \u2014 <code>num_ctx</code>, <code>num_gpu</code>, <code>keep_alive</code> \u2014 and a test fails the build if they differ. Ollama reloads a model when the context size changes, and for a VLM that discards the cached image prompt: 0.2 s becomes 132 s.</p> <p>The frame is capped at 640 px (<code>MINDER_VISION_MAX_WIDTH</code>). The main stream's 2560\u00d71440 is 4118 prompt tokens against a 4096 context, which Ollama answers with HTTP 400 \u2014 and the code read that error as \"unsure\". The capped frame is both faster and more accurate than the substream it replaced.</p>"},{"location":"architecture/#7-inference-what-runs-on-what-silicon","title":"7. Inference: what runs on what silicon","text":"<p><code>design/inference-backends.md</code></p> <p>Everything above is what Minder decides. This is where the arithmetic happens \u2014 the part that changes when Minder runs on somebody else's hardware.</p>"},{"location":"architecture/#71-two-roles-because-hardware-splits-that-way","title":"7.1 Two roles, because hardware splits that way","text":"<pre><code>Role            Input    Output          Served by\nObjectDetector  frame    list of boxes   CPU \u00b7 CUDA \u00b7 OpenVINO \u00b7 Coral \u00b7 RKNN \u00b7 Hailo\nEmbedder        crop     a vector        CPU \u00b7 CUDA \u00b7 OpenVINO \u00b7 RKNN\n</code></pre> <p>One interface with a <code>run()</code> is the tempting shape and the wrong one: a Coral Edge TPU runs a YOLO and cannot run a face embedder at all. A single interface lets a platform claim inference when it supports half of it, and the failure surfaces as an armed face plugin quietly finding nobody. Two roles means each capability is declared separately and a plugin needing an embedder on a detector-only box is refused at install, by name.</p>"},{"location":"architecture/#72-three-rules","title":"7.2 Three rules","text":"<ol> <li>Every role has a CPU implementation, always present. The degradation floor. A backend that    cannot load falls back to CPU and records why (<code>/data/inference_roles.json</code>, read by the health    page from any process). It never goes blind and never pretends to be the accelerator.</li> <li>The backend is chosen once, at the boundary, and injected. No module reads configuration at    import time. That was a real bug \u2014 the detector pinned itself to the CPU as a side effect of being    imported, before any configuration could be read.</li> <li>Backends return the same shapes, or they do not load. <code>inference/_types.py</code> is the contract.</li> </ol>"},{"location":"architecture/#73-the-floor-is-onnx-runtime","title":"7.3 The floor is ONNX Runtime","text":"<p>As PyTorch the floor was 1.66 GB to run one YOLOv8-n forward pass, on every platform including the ones with an NPU that never call it. As ONNX Runtime it is ~67 MB and cold start went from 13.0 s to 0.7 s. The cost: the model is exported at a fixed 640\u00d7640 because Coral and Hailo require fixed shapes, and across 9 frames \u00d7 3 class groups 23 of 27 results were bit-identical to PyTorch \u2014 the differences were a confidence at the second decimal and a duplicate box on a class no rule references. Accepted, measured, and re-checkable with <code>scripts/probe_onnx_floor.py</code>.</p>"},{"location":"architecture/#74-backends-are-drop-in-like-plugins","title":"7.4 Backends are drop-in, like plugins","text":"<p>A backend is a directory under <code>/data/backends/</code> (or <code>backend-library/</code> in the repo) with a manifest \u2014 which roles it serves, what it costs, what device it needs \u2014 and a module loaded by path under a unique name, because two backends both named <code>backend.py</code> once collided in <code>sys.modules</code>. <code>backend_install.py</code> installs, lists and removes; the builtin <code>cpu</code> cannot be removed.</p> <p>A backend is done when the conformance suite says so. <code>backend_verify.py</code> is what an OEM is handed as the definition of done: parity against the floor on real frames (a box shifted three pixels passes; a lost person fails; a box on a different object fails), declared cost against measured cost, roles it claims against roles it answers, degradation (detection continues and the failure is reported), and output shapes. It is tested against backends deliberately broken in each of those ways, because a suite that cannot fail is decoration.</p> <p>Rockchip was verified without the board. <code>rknn-toolkit2</code> ships a PC simulator; the RKNN backend converted, loaded and matched the floor numerically through the suite on a development machine. What a board still has to prove is timing and int8 quantisation \u2014 the reasons to have an NPU \u2014 and that is the only thing left to buy hardware for.</p>"},{"location":"architecture/#75-what-a-box-is-is-data","title":"7.5 What a box is, is data","text":"<p><code>/data/platform.yaml</code> says which backend serves each role, what detector Frigate should use, and what budget arming is measured against. Written at first boot by detection, overridable by shipping the file. The line that mattered most: Frigate's detector block used to be a hard-coded <code>cpu</code> literal on every machine, which was the entire camera-side cost of a port.</p> <p>Detection happens twice and the answers can disagree. Once on the host before any container exists (which image, which device passthrough, which host packages); once inside the container at first boot (which backend serves each role). Before the plan is applied they legitimately differ, and that difference is the most important thing the appliance can report: a machine with a GPU the container cannot reach looks exactly like a machine with no GPU \u2014 every internal check passes, consistently wrong, with a clean dashboard. So the host's answer is recorded (<code>MINDER_HOST_PLATFORM</code>) and compared, and when they differ the dashboard says so in words and names the file that fixes it.</p> What happens without the host driver NVIDIA the stack does not start \u2014 loud, immediate Rockchip, Coral, Hailo, Intel everything starts and silently runs on the processor, forever <p>The second row is what this whole section is against.</p>"},{"location":"architecture/#8-the-appliance-images-bootstrap-health","title":"8. The appliance: images, bootstrap, health","text":"<p><code>design/appliance-bootstrap.md</code>, <code>design/health-monitor.md</code></p> <p>One Dockerfile, three targets, selected not generated. <code>weights</code> exports the ONNX floor at build time; <code>base</code> carries the CPU floor and no PyTorch; <code>cpu</code> (the default) adds PyTorch for the one feature that still needs it \u2014 training site-specific detectors. Accelerators are compose overrides (<code>docker-compose.cuda.yml</code>, <code>.rknn</code>, <code>.coral</code>, <code>.hailo</code>, <code>.openvino</code>), mutually exclusive because a machine has one, and a test caps the target count so this stays a handful of images and not a matrix.</p> <p><code>./minder</code> is the CLI, and it is POSIX shell so it works before Python and Docker are set up. <code>bootstrap</code> detects the hardware, asks the few questions that matter and writes <code>.env</code> \u2014 nothing else; host toolkits (the NVIDIA container toolkit, the Rockchip driver) are explained and offered, never silently installed, with what breaks if they are absent. <code>up</code> builds first and returns when the runtime answers, not when Docker reports the container started \u2014 both were rules people had to remember, and both were forgotten in ways that produced \"verified\" results reading stale code. <code>status</code> shows what is running, on what silicon, and what is degraded.</p> <p>The health monitor reports; it does not restart. Every 45 s it probes what the supervisor cannot see \u2014 a process that is up but not working: SwarmKit on 8321, Frigate, Home Assistant, Ollama, the adapters, the poll loop \u2014 with hysteresis (two consecutive failures before down), and writes <code>/data/health.json</code> for the dashboard. On a state transition it publishes an alert over the bus, so \"Telegram blocked\" reaches you on Discord. It never kills anything: a service busy mid-inference must not be mistaken for a hung one. Alongside liveness it carries the rows this document keeps returning to \u2014 the inference role and its fallback reason, the platform-vs-container mismatch, Frigate's detector, the alerts the second opinion suppressed.</p> <p>The doctor is a read-only check on the health monitor's loop, every six hours and at startup: are the precious state files intact; if not, file a human-gated repair and say so. It is code, not a model run. Backups are nightly to a host bind mount (<code>backups/</code>), restored from the dashboard.</p>"},{"location":"architecture/#operating-it","title":"Operating it","text":"<pre><code>cd ~/minder\n./minder bootstrap                # first time: detect the hardware, write .env\n./minder up                       # build, start, wait until it can answer\n./minder status                   # what's running, on what silicon, what's degraded\n./minder logs minder              # watch\n./minder down                     # stop\n</code></pre> <p><code>./minder up</code> is the deployment command \u2014 not the raw compose start, which reports Running and ships nothing when the image has not changed. SwarmKit serves on 8321 (localhost only), the dashboard on 80; everything is <code>network_mode: host</code>, so nothing else on the machine may hold those ports. Telemetry is off by default (<code>MINDER_OTEL=otlp</code> exports to a collector on <code>:4318</code>); it once defaulted on and, with no collector, logged a connection error every 20 s until the real logs were unreadable.</p>"},{"location":"architecture/#9-scenario-studio-rules-in-words","title":"9. Scenario Studio: rules in words","text":"<p><code>design/scenario-studio.md</code> and its five phase notes.</p> <p>\"Turn on the porch light when you see a car at the gate after 10pm\" becomes:</p> <pre><code>{\"condition\": \"car\", \"cameras\": [\"Front-Gate\"], \"trigger_type\": \"vision\",\n \"actions\": [{\"type\": \"device\", \"device\": \"porch light\", \"action\": \"turn_on\"}],\n \"schedule\": \"22:00-06:00\"}\n</code></pre> <p>A rule is data \u2014 listed, inspected, deleted, shown back before it runs. The router produces the plan; <code>author_scenario</code> translates it; the rule engine in the Frigate server evaluates it with no language model at match time. The condition grammar grew by phase, each on the same machinery:</p> condition source note object presence (<code>person</code>, <code>car</code>, <code>animal</code>) Frigate events the original count threshold (\"more than two people\"), debounced Frigate's retained count topics phase 1; \"more than two\" parsed from words presence or count in a zone zones drawn on the dashboard \u2192 Frigate zones phase 1.5 zone crossing (entered / left) Frigate's zone transitions cross sensor state Home Assistant \u2014 time the clock <code>at_time</code> severity + VLM escalation the frame \u00a75.5 <p>Site-specific detectors (phase 2, \"quick mode\"): capture frames from a camera, label them \u2014 optionally with a cloud model's help \u2014 train a small detector off the box, import the weights, and run it in a background loop on demand. Training does not belong on an appliance; the toolchain that would need is the argument in one paragraph.</p>"},{"location":"architecture/#10-detection-plugins-and-face-recognition","title":"10. Detection plugins and face recognition","text":"<p><code>design/detection-plugins.md</code>, <code>design/face-recognition.md</code>, public doc <code>face-recognition.md</code></p> <p>Optional perception \u2014 faces today, cartons on a belt tomorrow \u2014 added without a rebuild and without touching the detection path. A plugin is a directory in <code>/data/plugins/</code> with a manifest, code, and a model fetched on install with a checksum; models are not baked into a ~3 GB image for a feature nobody may arm.</p> <p>Three levels of off, and they differ: not installed costs nothing; installed is authorable in rules and costs disk; armed has the model resident on a cadence and costs CPU. You can install something to look at it without paying for it, and <code>MINDER_PLUGINS=off</code> stops all of it.</p> <p>Declared cost makes arming safe. Every manifest states its cost as continuous load \u2014 180 ms every 5 s is 3.6% of a core \u2014 and arming is refused with a reason you can act on when the budget is spent, instead of you discovering it as Frigate dropping frames.</p> <p>Plugins are classified by what they remember, not what they detect, and governance follows the kind: a <code>detector</code> keeps nothing between frames; an <code>identifier</code> keeps templates identifying a person and brings enrolment, consent, retention and provable deletion with it (a licence plate identifies a person as surely as a face \u2014 ANPR is an identifier); a <code>tracker</code> keeps cross-frame state and a memory bound.</p> <p>A plugin may not set severity and may not suppress an alert \u2014 its return value has no field for the first, and the runner has no route to the alert path for the second, asserted on its source. A wrong match costs a wrong name, never a missed event.</p> <p>Frames on demand. Frigate detects on the substream \u2014 enough to find a person, not to identify one. A plugin that needs resolution declares it and gets one main-stream grab per detection, cropped to the box Frigate found: 185\u00d7398 out of 2560\u00d71440, 2% of the pixels, and the face detector runs in 9 ms instead of 453. The grab is deleted as soon as the plugins have looked.</p> <p>Face recognition is the first identifier and the one that shaped the governance. Enrolment is a human act on the dashboard; templates exist only for enrolled people; recognising a stranger is a first-class feature (\"alert me if an unrecognised face is at the door after dark\") and needs templates only for the people you know; there is no watchlist; a match annotates history and alerts and never unlocks, disarms or quietens anything; deletion is provable. Groups (\"family\", \"guests this week\") are the unit rules are written over, and guests expire.</p>"},{"location":"architecture/#11-memory-the-houses-history","title":"11. Memory: the house's history","text":"<p><code>design/queryable-events.md</code></p> <p>Before this, the only log was a 500-entry ring buffer written by <code>write_alert</code> \u2014 a thing was recorded if and only if a human was notified. \"Did anything happen at 3am that you didn't wake me for?\" was unanswerable against a log defined as the things it told you.</p> <p>Notification is a property of an observation, not the condition for recording one. One table across every source \u2014 camera, device, sensor, rule, system, channel \u2014 because every question worth asking crosses them (\"was the pump running when nobody was home?\"). Every row carries <code>notified</code>, and when false, why not (cooldown, unarmed, suppressed). SwarmKit's audit log is mirrored in, so device actions and doctor findings are in the same history as detections; routing decisions are recorded too (<code>router_log.py</code>), which is what makes the benchmark growable from real traffic.</p> <p>A negative answer is only safe if Minder was looking. <code>coverage.py</code> records every camera's online/offline transitions, and an answer to \"was anyone at the gate last night\" carries the caveat when the gate camera was down for two of those hours. Built on SwarmKit's storage seam, so the local SQLite store becomes Postgres with a URL, not a rewrite \u2014 and the appliance's local store is never optional; anything central is a replica.</p> <p>The query path is the same shape as everything else: the router parses \"what happened at the gate last night\" into a structured <code>Query</code>; code executes it; the answer is rows with a caption.</p>"},{"location":"architecture/#12-channels","title":"12. Channels","text":"<p><code>design/messaging-adapters.md</code>, <code>design/discord-qr-invite.md</code>, <code>design/onboarding-webapp.md</code></p> <p>Adapters are thin. Telegram and Discord bots contain no logic; every message goes to <code>POST /api/ops/message</code> and every reply is a structured envelope (<code>kind</code>, <code>text</code>, <code>media</code>) the adapter renders its own way. Adding a channel is a subscriber and a renderer, never a change to the detection path. This exists because India blocked Telegram and the product needed a second channel without a rewrite.</p> <p>Alerts fan out over MQTT with a durable queue per subscriber. Minder publishes once to <code>minder/alerts</code>; each adapter is a persistent-session, QoS 1 consumer with a fixed client id, so an adapter that was down gets its backlog when it returns, and a blocked channel never blocks the others. The dashboard is a subscriber too.</p> <p>The dashboard (port 80) is onboarding \u2014 discover cameras, pair Home Assistant, choose a channel, pair a phone with a QR invite \u2014 and the operator's view: health, rules, zones, enrolled faces, history, backups. Session-cookie auth; the ops API is also reachable by the adapters with an internal token that never leaves the box.</p>"},{"location":"architecture/#13-state-tests-and-the-discipline-around-them","title":"13. State, tests, and the discipline around them","text":""},{"location":"architecture/#state","title":"State","text":"Where What <code>minder_minder-data</code> volume \u2192 <code>/data</code> cameras, rules, zones, channel config, monitor state, <code>platform.yaml</code>, <code>inference_roles.json</code>, <code>health.json</code>, <code>alerts_suppressed.json</code>, <code>router_feedback.json</code>, plugins, backends, media <code>minder_ha-config</code> volume Home Assistant's own configuration <code>minder_frigate-media</code> volume recordings (two days) and snapshots <code>backups/</code> (bind mount) nightly backups, on the host <code>workspace/.swarmkit/</code> audit log, run state, the observation store <p>The compose project name is pinned to <code>minder</code> because volume names carry it; an unpinned project in a renamed directory silently creates new empty volumes while the real ones sit on disk \u2014 a wiped-appliance-shaped failure with a one-line cause.</p>"},{"location":"architecture/#tests","title":"Tests","text":"<p>~700 tests run inside the image in CI (<code>.github/workflows/ci.yml</code>), because many reference <code>/app</code> paths; a handful that share module state run in their own process. Beside the unit tests is a second kind, each written after the failure it now prevents: guards that assert the system describes itself honestly. Every environment variable the code reads is passed by compose (<code>test_env_plumbing</code> \u2014 three variables were once documented and read by nothing); every root module is in the image (<code>test_image_contents</code> \u2014 one was missing and only a deploy found it); the model inventory matches the repository (<code>test_models_doc</code>); the vision archetypes and the router agree on residency options; no document instructs a deployment command that ships nothing; the Dockerfile has one entrypoint and a capped number of targets; pre-routing is 100% precise on every labelled case.</p>"},{"location":"architecture/#benchmarks-are-the-other-half-of-the-tests","title":"Benchmarks are the other half of the tests","text":"<p><code>scripts/</code> holds a benchmark for every decision that was measured: the router (<code>bench_router*.py</code>), VLM models (<code>bench_vlm_models.py</code>), the describe prompt (<code>bench_describe*.py</code>), the confirm schema, needle-in-frame ablations, the ONNX floor's parity, the seam's parity against the model it replaced. They are parameterised so the next change re-runs them instead of inheriting a conclusion, and <code>bench_isolated.sh</code> is the only way they run on the appliance.</p>"},{"location":"architecture/#14-decisions-that-were-reversed","title":"14. Decisions that were reversed","text":"<p>Each looked obviously right until it was measured. A system that hides its reversals teaches the next person the wrong lesson, so they are collected here with the number that reversed them.</p> Was Is What changed it VLM on the CPU \u2014 the GPU would thrash VLM on the GPU A real question grabs a fresh frame and never hits the prompt cache: 131.8 s against 18.0 s. Every prior number had reused a stored snapshot. The router on its own model (<code>qwen2.5:3b</code>, then <code>qwen3.5:4b</code> planned) The router is the VLM, <code>gemma3:4b</code> One 4B model fits the card; a second evicts the first on every message. Measured through the topology: 94.0% guarded at 2.1 s, resident. A bigger router model is the fix for misroutes The prompt and the taxonomy are The prompt fixes moved every candidate 2\u20139 points; the model swap alone moved nothing. Few-shot examples from the case set will help a small model They hurt it The nearest phrasings are the confusable ones; \u22126 points raw on gemma3. Residency is about <code>num_ctx</code> It is about <code>num_gpu</code> llama.cpp reserves ~1.7 GB for a multimodal projector; the model was half on the CPU at every context size until pinned. Alert on Frigate's word A second opinion first Frigate's CPU detector scores a fence post 0.71, the same as a person; the seam finds 0 of 20 false positives to be one. Describe the alert snapshot Describe a 640 px main-stream frame, told where to look The snapshot is 352 px with a box burned in; the two-sentence prompt on the better frame added detail and invented nothing. Keep camera frames low-resolution for the VLM Grab full-resolution, cap the longest edge at 640 Full resolution exceeded the context and returned an error read as \"unsure\"; the cap is faster and more accurate. The describe prompt belongs in the archetype's system message It stays in code, inline with the situation Moved, the model read timestamp overlays and invented entities; it honours a system message only under a grammar. PyTorch is the CPU inference floor ONNX Runtime is 1.66 GB against 67 MB, paid on every platform including those that never call it. A backend that falls back is fine if it works It must say so An accelerator silently not used is indistinguishable from not having one. A clean dashboard means a healthy box Only if something claimed an accelerator A GPU the container could not reach produced perfect health; the host's own detection is now compared. \"Dark shadows\" cause the porch false alerts A fence post at the frame edge and a parked car's bulk Forty snapshots on one sheet, in a minute; the numbers alone would not have shown it. <p>Every one of these is a property of the specific model or machine, not a general truth.</p>"},{"location":"architecture/#15-where-to-read-next","title":"15. Where to read next","text":"Question Where What did each design note decide, and did it ship? <code>design/README.md</code> Why does the router work this way, and what did it measure? <code>design/conversation-router.md</code>, <code>design/alert-quality.md</code> \u00a73 Why is there a second opinion before an alert? <code>design/alert-quality.md</code> \u00a71 How do the two vision layers split, and why GPU? <code>design/two-layer-vision-models.md</code>, \u00a76 above Why is inference behind a seam, and what are the two roles? <code>design/inference-backends.md</code> How does Minder get onto new hardware? <code>design/appliance-bootstrap.md</code> How are rules authored and evaluated? <code>design/scenario-studio.md</code> and its phase notes How do plugins work, and what may they not do? <code>design/detection-plugins.md</code> What does face recognition store, and how do I delete it? <code>face-recognition.md</code> How does the house's history work? <code>design/queryable-events.md</code> How do channels plug in? <code>design/messaging-adapters.md</code> Which model runs where, and what sets it? <code>MODELS.md</code> (generated) The invariants that are not obvious from the code <code>CLAUDE.md</code>"},{"location":"detector-tuning/","title":"Detector tuning: how the models were tested, and why resolution mattered more than the model","text":"<p>This is the record of one deployment's detector work \u2014 what was measured, how, and what changed \u2014 written so the next installation can repeat the measurements rather than repeat the mistakes. Every number below came from the reference house: seven cameras, an 8-core CPU, no GPU that Frigate could use, and the appliance running in WSL2 (no <code>/dev/dri</code>).</p> <p>The one-line version: a better model on a 352\u00d7288 frame is still blind to a person 25 m away. The stream you detect on matters as much as the network you detect with, and a 300 px face is invisible to the face finder in the other direction. Measure your own cameras before you trust either default.</p>"},{"location":"detector-tuning/#the-three-measurements","title":"The three measurements","text":"<p>There are three ways to test a detector change, and each answers a different question. Do all three; each one caught something the others did not.</p>"},{"location":"detector-tuning/#1-microbenchmark-what-does-it-cost","title":"1. Microbenchmark \u2014 \"what does it cost?\"","text":"<p>Random input through the model in the same runtime Frigate uses, inside the Frigate container:</p> <pre><code>import time\n\nimport numpy as np\nimport openvino as ov\n\ncore = ov.Core()\nm = core.compile_model(core.read_model(\"/config/model_cache/yolov8n-320.onnx\"), \"CPU\")\nx = np.random.rand(1, 3, 320, 320).astype(np.float32)\nfor _ in range(3):\n    m(x)  # warm up\nt = time.time()\nfor _ in range(30):\n    m(x)\nprint((time.time() - t) / 30 * 1000, \"ms\")  # 28 ms at 320, 46 ms at 416 on this box\n</code></pre> <p>Frigate's own <code>inference_speed</code> in <code>GET /api/stats</code> is the same number measured live (17\u201322 ms once the graph was warm), and <code>docker stats</code> gives the container's total.</p> <p>What it tells you: whether the box can afford the change. Seven cameras at 5 fps is a worst case of 35 detections/s; at 28 ms that is one core, at 46 ms it is not. What it does not tell you: whether the detector sees anything.</p>"},{"location":"detector-tuning/#2-offline-recall-does-it-see-what-the-old-one-saw","title":"2. Offline recall \u2014 \"does it see what the old one saw?\"","text":"<p>Frigate keeps a snapshot for every event, at the detect resolution. That is a free labelled set: whatever the previous detector called <code>person</code> over the last day, run through the new model offline, and count how many it agrees with. Contact-sheet the snapshots first (<code>scripts/audit_events.py events</code>) \u2014 some of what the old detector called a person is a fence post, and you are measuring the new model against the real ones.</p> <pre><code>GET /api/events?after=&lt;yesterday&gt;&amp;labels=person&amp;limit=400   \u2192 snapshot per event\nnew model on each snapshot, at conf 0.3 / 0.5 / 0.7            \u2192 best person score\n</code></pre> <p>This is the measurement that caught the regression. YOLOv8-n at 320 is a far better network than Frigate's default SSDLite \u2014 and on the substream it found 0 of 38 children cycling past the far gate that SSDLite had scored 0.69\u20130.76:</p> camera (substream 352\u00d7288) real <code>person</code> snapshots YOLO-320 \u2265 0.7 \u2265 0.5 \u2265 0.3 far gate (cyclists, ~20 px tall) 38 0 0\u20131 1\u20132 front door (top-down, ~60 px) 82 11\u201319 16\u201329 29\u201347 porch 10 (mostly one phantom) 0 0 0 <p>(The ranges are stretch vs letterbox resize; letterboxing helped the door a little and the gate not at all.) A 20 px person is below what a COCO-trained detector at 320 resolves, full stop. The old model scoring them 0.7 was not skill \u2014 it scored a parked car 0.71 too \u2014 but it was recall, and the new model had none. Had this gone straight to production the far gate would have gone quiet and looked \"fixed\".</p>"},{"location":"detector-tuning/#3-live-what-does-it-do-on-the-real-stream","title":"3. Live \u2014 \"what does it do on the real stream?\"","text":"<p>Switch it on, and read three things for twenty minutes:</p> <ul> <li><code>GET /api/stats</code>: <code>inference_speed</code>, and per camera <code>detection_fps</code> against <code>camera_fps</code>.</li> <li><code>GET /api/&lt;camera&gt;/latest.jpg?bbox=1&amp;regions=1&amp;motion=1</code>: what is boxed, and where the motion   regions are.</li> <li>Events: what fired, against the same hour the day before.</li> </ul> <p>The live view is what showed the phantom was gone (two cars boxed as cars at 0.82\u20130.87, no <code>person</code> on either) \u2014 and, later, the side effect of the stream change below.</p>"},{"location":"detector-tuning/#why-resolution-mattered-more-than-the-model","title":"Why resolution mattered more than the model","text":"<p>Frigate does not run the detector on the whole frame. It finds motion, cuts a region around it (never smaller than the model's input, 320 px), and resizes the region to the model. On a 352\u00d7288 substream the region is the frame: a person 20 px tall stays 20 px. On the same camera's main stream scaled to 1280\u00d7720, the region is a 320 px crop at that resolution, and the same person is ~70 px inside it \u2014 a size any COCO detector handles.</p> <p>So the fix was not a third model; it was telling Frigate to detect on the main stream for the cameras that watch a far approach:</p> <pre><code>{ \"name\": \"Front-Gate\", \"detect\": { \"stream\": \"main\", \"width\": 1280, \"height\": 720 } }\n</code></pre> <p>(the Detect on select on the dashboard camera card; <code>MINDER_DETECT_STREAM=main</code> for every camera). What it cost and what it gave, measured:</p> substream 352\u00d7288 main stream \u2192 1280\u00d7720 decode (software, per camera) ~0.07 core ~0.3 core detection rate when nothing is happening ~5 fps, continuously \u2014 compression noise trips the motion gate 0.2 fps a person 25 m away, in the region the model sees 20 px ~70 px <p>The second row was unexpected and pays for most of the first: at 352\u00d7288 the motion detector fires on noise all day, so every camera was running the detector at full rate all the time. At 1280\u00d7720 it fires when something moves. Per camera, because most cameras do not need it \u2014 the door camera has 60 px faces on the substream already \u2014 and each one on the main stream is a third of a core.</p>"},{"location":"detector-tuning/#the-same-lesson-the-other-way-round-faces","title":"The same lesson, the other way round: faces","text":"<p>The face finder (YuNet) had the opposite problem. It finds a face at 250 px and finds nothing at 400 px or above \u2014 measured on the same face, scaled:</p> image face found 640 px 247 px yes 1024 px 396 px no 2000\u20134000 px 770\u20131550 px no <p>A phone photograph has a 1200 px face, so every enrolment photograph came back \"no face found\", though the same model finds a 90 px face at the door every time. The fix (<code>face_align.py</code>, <code>detect_any_size</code>) detects on a copy no wider than 640 px and maps the landmarks back to the full-resolution photograph, so alignment still reads the real pixels. Camera frames pass through unchanged. Same rule as above: know the size envelope of the model, and put the pixels inside it \u2014 down for the phone, up for the gate.</p>"},{"location":"detector-tuning/#checklist-for-a-new-deployment","title":"Checklist for a new deployment","text":"<ol> <li>Microbenchmark the candidate in Frigate's container. Multiply by cameras \u00d7 fps; if that is    more than a core or two, stop here.</li> <li>Contact-sheet a day of events per camera. Note which cameras have far subjects (a person    under ~40 px in the substream) and which have phantoms.</li> <li>Offline recall of the candidate over the real <code>person</code> snapshots. A camera where it drops    to zero needs the main stream, not a different model.</li> <li>Switch on per camera: the model in <code>/data/platform.yaml</code> (<code>frigate.detector</code> +    <code>frigate.model</code>), the stream per camera record. <code>./minder up</code> or the dashboard applies it.</li> <li>Live for twenty minutes: <code>/api/stats</code>, the debug frame, events vs the same hour yesterday.    Then a walk past each far camera, in daylight and at night.</li> <li>Keep the second opinion on. A better detector is still not a witness; the seam's look at the    640 px frame before an alert is what keeps a phantom from reaching your phone.</li> </ol> <p>The numbers above are one house's. Yours will differ in the direction that matters \u2014 which cameras are far, how noisy the substream is at night \u2014 and the three measurements take an hour.</p>"},{"location":"face-recognition/","title":"Face detection and recognition","text":"<p>Two optional plugins. Together they let Minder say \"Alex is at the main door\" instead of \"person at the main door\" \u2014 and, more usefully, \"someone unrecognised is at the gate\".</p> <p>Neither is installed by default. Neither runs until you arm it.</p> Plugin What it does Keeps <code>face-detect</code> says a face was visible, and how big nothing <code>face-recognise</code> puts a name to faces of people you enrolled one template per enrolled person <p>If you only want \"was there a face?\", install the first and stop there. It stores nothing about anybody.</p>"},{"location":"face-recognition/#turning-it-on","title":"Turning it on","text":"<p>Three steps, and nothing happens until all three are done.</p> <p>1. Install the plugins. Models are fetched and checksum-verified at this point, not baked into the image.</p> <pre><code>docker compose exec minder python -c \"\nimport plugin_install as pi\nprint(pi.install('face-detect'))\nprint(pi.install('face-recognise'))\"\n</code></pre> <p>Installing does not start anything. It costs disk and nothing else.</p> <p>2. Arm them. This is what loads the models and starts the work.</p> <pre><code>docker compose exec minder python -c \"\nimport plugins\nr = plugins.registry()\nfor p in ('face-detect', 'face-recognise'):\n    ok, why = r.arm(p)\n    print(p, '- armed' if ok else '- refused: ' + why)\"\n</code></pre> <p>If your CPU budget is already spent, this refuses and says by how much rather than quietly overloading the box.</p> <p>3. Switch plugins on for the appliance. In <code>.env</code>:</p> <pre><code>MINDER_PLUGINS=on\n</code></pre> <p>then <code>./minder up</code>. This is the master switch \u2014 off by default, because optional perception on a machine whose first job is recording your house should be turned on deliberately.</p>"},{"location":"face-recognition/#check-it-worked","title":"Check it worked","text":"<pre><code>docker compose exec minder python -c \"\nimport plugins, plugin_bridge\nr = plugins.registry()\nprint('plugins enabled:', plugin_bridge.ENABLED)\nprint('armed          :', [m.id for m in r.armed()])\nprint('cpu budget used:', f'{r.used_core_fraction():.1%}')\"\n</code></pre> <p>Then enrol someone at Portal \u2192 People, and the next time a camera sees a person Minder will try to name them.</p>"},{"location":"face-recognition/#the-rule-that-matters-most","title":"The rule that matters most","text":"<p>A recognised face never silences an alert. It can change the words \u2014 \"Alex at the main door\" instead of \"person at the main door\" \u2014 but it cannot make an alert quieter, later, or absent.</p> <p>So if recognition gets it wrong, you get a wrong name, never a missed intruder. Your security does not depend on a face model being right.</p> <p>That means \"don't alert me when it's me\" is deliberately not built in. If you want it, you write it as a rule you can see and switch off \u2014 not as a hidden side effect of turning recognition on.</p>"},{"location":"face-recognition/#what-is-stored-and-what-is-not","title":"What is stored, and what is not","text":"<p>For each person you enrol:</p> Stored Not stored a mathematical template (512 numbers) the photograph \u2014 discarded once the template is made their name and group anything about people you did not enrol who enrolled them, and when any record linking one stranger to another sighting <p>The photograph goes as soon as the template is computed, including when enrolment fails. A template is not a picture and cannot be turned back into one.</p> <p>Nobody else gets a template \u2014 ever. An unrecognised face is recorded as \"a face, matched nobody\", and nothing about that person is kept.</p>"},{"location":"face-recognition/#the-cost-of-that-stated-plainly","title":"The cost of that, stated plainly","text":"<p>You can ask: \"was an unrecognised person at the gate last night?\" You cannot ask: \"was that the same stranger as Tuesday?\"</p> <p>Answering the second would mean keeping face templates of people who never agreed to it. That is the trade, and it was chosen deliberately.</p>"},{"location":"face-recognition/#groups","title":"Groups","text":"<p>Everyone you enrol belongs to one group. Rules use the group, not the name \u2014 so \"anyone who isn't family\" keeps working the day a cousin visits.</p> Group Typically <code>family</code> lives here; any hour is unremarkable <code>household</code> house help, nanny \u2014 expected, during known hours <code>service</code> gardener, cleaner, delivery <code>guest</code> temporary \u2014 expires, see below (no match) stranger \u2014 not a group, the absence of one <p>You can add your own groups. A stranger needs no enrolment and no consent \u2014 it is simply a face that matched nobody, which is why stranger rules work for people who have never been near your house.</p> <p>Guests expire. A guest needs an end date, and the template is deleted automatically after it. Someone enrolled for a fortnight's visit will not still be recognised two years later.</p>"},{"location":"face-recognition/#enrolling-someone","title":"Enrolling someone","text":"<p>Portal \u2192 People tab. One person at a time, from photographs \u2014 three to five, different angles and lighting, each with their face at least 80 pixels across. Nobody has to stand in front of a camera: enrol at the desk the way a reception would, and every camera recognises them.</p> <p>Enrolment only works from a signed-in browser session. Minder's own internal components cannot do it, and neither can the chat bot \u2014 storing someone's face is a decision a person makes, and the record says who made it.</p> <p>A photo with two faces in it is refused rather than guessed at.</p>"},{"location":"face-recognition/#deleting-someone","title":"Deleting someone","text":"<p>Portal \u2192 People \u2192 Delete. Minder then:</p> <ol> <li>removes every template for them;</li> <li>clears their name from past events \u2014 the events stay, only the identification is withdrawn;</li> <li>records the deletion, with who did it and how many things it touched;</li> <li>re-checks both stores and shows you the result.</li> </ol> <p>Step 4 is the point. The screen reports what a re-query found, not that a request succeeded.</p>"},{"location":"face-recognition/#writing-rules","title":"Writing rules","text":"<p>Add to <code>/data/rules.json</code>, or use the Rules tab. Hours are optional \u2014 state them only if the rule cares.</p> <p>Alert if a stranger appears at night</p> <pre><code>{\"condition_type\": \"face_identity\", \"condition\": \"unrecognised person at night\",\n \"cameras\": [\"all\"], \"schedule\": \"22:00-06:00\", \"stranger\": true, \"enabled\": true}\n</code></pre> <p>Alert if anyone who is not family is here after 10pm</p> <pre><code>{\"condition_type\": \"face_identity\", \"condition\": \"not family, late\",\n \"cameras\": [\"all\"], \"schedule\": \"22:00-06:00\", \"group_not\": \"family\", \"enabled\": true}\n</code></pre> <p>Strangers count as \"not family\" \u2014 which is what you want, and would otherwise make the rule useless in the case you wrote it for.</p> <p>Tell me when the gardener arrives</p> <pre><code>{\"condition_type\": \"face_identity\", \"condition\": \"gardener here\",\n \"cameras\": [\"Front-Door\"], \"identity\": \"Ramesh\", \"enabled\": true}\n</code></pre> Key Matches <code>stranger: true</code> a face that was looked at properly and matched nobody <code>group_not: \"family\"</code> anyone not in that group, including strangers <code>group: \"service\"</code> enrolled people in that group <code>identity: \"Ramesh\"</code> that person <code>schedule</code> <code>always</code>, <code>night</code>, <code>day</code>, or <code>22:00-06:00</code>. Optional"},{"location":"face-recognition/#combining-with-what-minder-can-already-see","title":"Combining with what Minder can already see","text":"<p>\"Alert if a stranger is carrying something\" is two things, not one: the face plugin says a face matched nobody, and that triggers the second vision layer with a specific question about what the person is holding.</p> <p>Worth doing in that order \u2014 asking a vision model \"is anyone dangerous?\" of every frame is vague and expensive; asking \"what is this person carrying?\" of the rare 2am frame with an unrecognised face is cheap and sharp. The face plugin's job is turning a general question into a rare one.</p>"},{"location":"face-recognition/#three-answers-not-two","title":"Three answers, not two","text":"Minder says Meaning a name that is who it is unrecognised a face, looked at properly, matching nobody too small to identify a face nobody could have identified <p>The third is not a stranger, and does not fire stranger rules. Otherwise every distant figure at the end of a driveway would raise \"unrecognised person\", and you would turn the rule off within a week.</p>"},{"location":"face-recognition/#what-it-costs","title":"What it costs","text":"<p>Measured on the reference appliance (Intel N100, 8 cameras):</p> grabbing a high-resolution frame 1.2\u20131.5s finding faces in it ~9ms* recognising one ~170ms while armed ~5% of one CPU core <p>* Because Minder crops to the person Frigate already found: 2% of the pixels, at full resolution \u2014 50x faster than scanning the whole frame, and sharper than shrinking it.</p> <p>The frame grab dominates, and it only happens when a camera has already detected somebody. Nothing runs continuously. Recognition looks at a short burst of five frames half a second apart rather than one \u2014 the first frame after a detection is usually the worst one \u2014 and reports the best.</p> <p>Your cameras' detection stream is 352x288, where a face is 15\u201325 pixels \u2014 too small to recognise anyone. That is why these plugins pull a full-resolution frame on demand. A camera whose main stream is unreachable cannot serve them, and the plugin list says which.</p>"},{"location":"face-recognition/#turning-it-off","title":"Turning it off","text":"<p>Three different \"off\", and they mean different things:</p> Effect Disarm stops running; stays installed and configured Uninstall removes the plugin and its model; enrolled people remain <code>MINDER_PLUGINS=off</code> stops all plugins, whatever is armed <p>To remove the biometric data itself, delete the people \u2014 uninstalling the plugin does not, on purpose, so that reinstalling does not silently lose them.</p>"},{"location":"face-recognition/#privacy-summary","title":"Privacy summary","text":"<ul> <li>Runs entirely on your appliance. No face ever leaves the house.</li> <li>Templates exist only for people someone deliberately enrolled.</li> <li>Photographs are discarded; templates cannot be turned back into faces.</li> <li>Deletion is verified, not asserted.</li> <li>A match can add a name to an alert. It can never remove one.</li> </ul>"},{"location":"privacy/","title":"Privacy","text":"<p>Minder watches your home. That makes what it keeps, and what leaves the house, the most important thing about it \u2014 so this page is specific rather than reassuring.</p>"},{"location":"privacy/#what-never-leaves-your-house-by-default","title":"What never leaves your house by default","text":"<p>Camera frames. Detection, description and recognition all run on the appliance. The vision model is local. Nothing is uploaded to anybody.</p> <p>Your recordings. Frigate stores clips and snapshots on the machine's own disk.</p> <p>Face templates, if you use recognition. They are computed on the appliance and stored there.</p> <p>Your conversations with it. The language model that reads your messages runs locally too.</p>"},{"location":"privacy/#what-can-leave-only-if-you-turn-it-on","title":"What can leave, only if you turn it on","text":"<p>Two features are opt-in, off by default, and independent of each other:</p> Feature Setting What is sent Cloud description of an alert frame <code>MINDER_DESCRIBE_PROVIDER=openrouter</code> that one snapshot Cloud answers to visual questions <code>MINDER_QUERY_PROVIDER=openrouter</code> that one snapshot <p>Both fall back to the local model if the cloud is unreachable \u2014 a cloud outage degrades quality, it never stops the appliance working.</p> <p>Alerts themselves go out through Telegram or Discord, which is a deliberate choice you make when you connect a channel: the message and its snapshot pass through that provider like any other chat. If that matters to you, both are optional and Minder works without either.</p>"},{"location":"privacy/#face-recognition-if-you-enable-it","title":"Face recognition, if you enable it","text":"<p>Not installed by default and does nothing until you arm it.</p> <p>A template exists only because someone deliberately enrolled that person, through a signed-in browser session. There is no chat command, no API token, and no automated path that can enrol anybody \u2014 the record names the human who did it and when.</p> Stored Not stored a mathematical template (128 numbers) the photograph \u2014 discarded once the template is computed the person's name and group anything at all about people you did not enrol who enrolled them, and when any link between one stranger and another sighting <p>A template is not a picture and cannot be turned back into one.</p>"},{"location":"privacy/#strangers","title":"Strangers","text":"<p>An unrecognised face is recorded as \"a face, matched nobody\" and nothing about that person is kept. That is why \"was an unrecognised person at the gate last night?\" can be answered while \"was that the same stranger as Tuesday?\" cannot \u2014 answering the second would mean keeping biometrics of people who never agreed to it.</p> <p>That trade was chosen deliberately, and it is the difference between a system that recognises the people you invited and one that recognises everybody who walks past.</p>"},{"location":"privacy/#deleting-someone","title":"Deleting someone","text":"<p>Deletion removes every template, clears their name from past events \u2014 the events remain, only the identification is withdrawn \u2014 records what it did, and then re-queries both stores and shows you the result. The screen reports what the check found, not that a request succeeded.</p> <p>Guests carry an expiry and are deleted automatically. Someone enrolled for a fortnight's visit will not still be recognised two years later because nobody remembered.</p>"},{"location":"privacy/#what-the-appliance-keeps-about-you","title":"What the appliance keeps about you","text":"Where Retention clips and snapshots Frigate, local disk 7 days by default the event history a local database until you delete it every message you send it the same database, so misroutes can be found <code>MINDER_ROUTER_LOG=off</code> disables face templates a separate local database until deleted, or a guest expires <p>Face templates live in their own database rather than alongside events, so \"delete everything biometric\" is one file rather than a search.</p>"},{"location":"privacy/#turning-things-off","title":"Turning things off","text":"Effect <code>MINDER_PLUGINS=off</code> all detection plugins stop, whatever is armed <code>MINDER_ROUTER_LOG=off</code> messages are no longer recorded disarm a plugin it stops running, stays installed delete a person templates gone, verified unplug it it is your machine"},{"location":"privacy/#the-honest-caveats","title":"The honest caveats","text":"<p>Alert channels are third parties. Telegram and Discord see the alerts you route through them.</p> <p>The portal guards all of this. It is reachable on your local network and protected by a password you set at install. If you use face recognition, that password is what stands between someone on your network and your household's biometric data \u2014 change it from the default.</p> <p>Cloud features send a real frame. When enabled, the snapshot going to the provider is an actual picture of whatever triggered the alert.</p>"},{"location":"using-minder/","title":"Using Minder","text":"<p>Minder watches your cameras, answers questions about what it sees, and controls your smart devices \u2014 over Telegram or Discord. This page is everything you need as a user. Nothing here requires reading code.</p>"},{"location":"using-minder/#before-you-start","title":"Before you start","text":"<p>You need:</p> <ul> <li>A machine that stays on \u2014 a mini PC, a NUC, an old laptop. Linux (this is developed on Ubuntu   under WSL2 and on bare Ubuntu). See Hardware below for what it needs; the short   version is 4+ cores, 16GB RAM, and a graphics card is optional.</li> <li>Cameras that speak RTSP. Almost all IP cameras do. You will need their username and password.</li> <li>A Telegram or Discord account to talk to it.</li> </ul> <p>Optional but recommended: a Home Assistant-compatible smart device or two, if you want Minder to switch things on and off. Home Assistant itself is installed for you \u2014 it is part of the stack.</p> <p>Also optional: an AI accelerator \u2014 an NVIDIA GPU, an Intel iGPU, a Coral or Hailo stick, or a board with a built-in NPU. Minder runs perfectly well without one. If you have one, the setup in step 5 finds it, tells you what else it needs, and \u2014 importantly \u2014 tells you when it is not being used, which otherwise looks exactly like not having one.</p>"},{"location":"using-minder/#hardware","title":"Hardware","text":"<p>Minder runs one local model (gemma3:4b) and Frigate's detector on the CPU, so the machine needs less than a first guess suggests. These are measurements from the reference house \u2014 seven cameras, two of them detecting on their full-resolution stream, face recognition on, an 8-core i7-8750H with a 4GB GTX 1050 Ti \u2014 not estimates:</p> measured what drives it graphics memory 2.9GB the one model, resident; the 4GB card runs at 3.7GB used CPU ~3.5 cores Frigate: video decode (~0.3 core per camera on the main stream, ~0.1 on the substream) plus the detector (~1 core); Minder itself idles at 0.1 RAM ~3.5GB all containers; add ~3GB if the model runs on the CPU disk ~1GB/day/camera recordings, at the default two-day retention <p>Three builds that work:</p> machine what you get Budget, no graphics card Intel N100/N305 mini PC, 16GB RAM, 512GB NVMe Everything works. The model runs on the CPU, so a description takes ~30s instead of ~7s and a routed answer 5\u20138s. Fine for up to about six cameras on their substreams. Recommended any 6-core Intel with an iGPU (i5-8400 or newer, i5-12400), a used 6GB NVIDIA card (GTX 1660, RTX 3050), 16GB RAM, 1TB NVMe This is the reference box's class. Seven cameras, two on full-resolution detection, the card mostly idle, ~3GB of graphics memory spare for a longer context or a bigger model later. On bare Ubuntu the Intel iGPU also takes the video decode, which is most of the CPU load. Headroom the same with an RTX 3060 12GB Only if you intend to run a 12B vision model or higher-resolution description without the cloud. Today ~9GB of it would sit idle. <p>Three things that are not true any more, in case you read them elsewhere: you do not need two models pulled; you do not need a 12GB card; and the graphics card does not help Frigate's detection \u2014 that runs on the CPU (OpenVINO), where it costs 17ms a frame on the reference box. A 4GB card is proven; 6GB is the sweet spot; RAM is the thing not to skimp on.</p>"},{"location":"using-minder/#installing","title":"Installing","text":""},{"location":"using-minder/#1-docker","title":"1. Docker","text":"<pre><code>curl -fsSL https://get.docker.com | sh\nsudo usermod -aG docker $USER      # so you don't need sudo for docker\nnewgrp docker                      # or just log out and back in\n</code></pre> <p>Check it works: <code>docker run --rm hello-world</code>.</p>"},{"location":"using-minder/#2-ollama-and-one-model","title":"2. Ollama, and one model","text":"<p>Minder's language and vision model runs on the host, not in a container \u2014 that is what keeps your camera images inside the house.</p> <pre><code>curl -fsSL https://ollama.com/install.sh | sh\n\nollama pull gemma3:4b           # looks at camera frames AND works out what you are asking for\n</code></pre> <p>About 3.3GB. One model, on purpose: it works out what you are asking, chats, and looks at frames, and a 4GB graphics card holds one model of this size \u2014 two would evict each other on every message. Setup, camera discovery, device control and the health check are plain code and need no model at all; the appliance sets itself up with Ollama not yet running. It runs on CPU; no GPU required \u2014 a GPU makes the vision answers roughly four times faster, and Minder will use one if you have it (see step 5).</p> <p>These are the defaults and you can change any of them later, but do not guess at substitutes: the vision model in particular was chosen on a benchmark over real camera frames, and the obvious alternatives failed in ways that matter. One faster, smaller model described \"a nighttime scene of a porch with a lit lamp and a door\" on a frame that was a blank error screen. <code>MODELS.md</code> in the repository has the comparison.</p> <p>Use a recent Ollama if you can \u2014 0.32 or newer. Upgrading from 0.21 made Minder's routing more than twice as fast on the same model, and older builds quietly ignore the structured-output format for newer model families, which makes them look broken when they are not.</p> <p>Skipping this? You can run entirely on cloud AI instead \u2014 set <code>OPENROUTER_API_KEY</code> in step 4 and <code>MINDER_DESCRIBE_PROVIDER=openrouter</code>. It is faster and more detailed, but camera snapshots then leave your network.</p>"},{"location":"using-minder/#3-get-minder","title":"3. Get Minder","text":"<p>Minder is not open source. The repository is private, and this step needs an account that has been granted access \u2014 if <code>git clone</code> answers <code>Repository not found</code>, that is what it means, not a broken link. Everything else on this page is readable without it.</p> <pre><code>git clone git@github.com:delivstat/minder.git ~/minder\ncd ~/minder\n</code></pre> <p>Keep the directory named <code>minder</code>. Docker names your data after it, so a different name means Minder comes up empty and looks like it lost everything.</p>"},{"location":"using-minder/#4-configure","title":"4. Configure","text":"<pre><code>cp .env.example .env\nnano .env\n</code></pre> <p><code>.env.example</code> is the short list. Every variable the stack takes \u2014 87 of them, with the default and the reasoning \u2014 is in <code>env.sample</code>, generated from the compose file so it is always current; copy a line from there into <code>.env</code> to change it.</p> <p>You can genuinely leave all of it blank on a first run \u2014 the web interface auto-detects your network and asks for camera credentials during onboarding. Fill these in only if discovery does not find your cameras:</p> <pre><code>MINDER_SUBNET=192.168.0    # the first THREE octets of your network, no /24 and no trailing .0\nMINDER_CAM_USER=admin      # your cameras' login\nMINDER_CAM_PASS=...\n</code></pre> <p><code>MINDER_SUBNET</code> is a prefix, not CIDR: Minder appends <code>.0/24</code> itself, so <code>192.168.0</code> scans <code>192.168.0.0/24</code>. Run <code>hostname -I</code> and drop the last number to get yours.</p> <p>Telegram, Home Assistant and cloud AI are all set up later through the web interface \u2014 nothing here needs them.</p> <pre><code>chmod 600 .env                 # it will hold passwords and tokens\n</code></pre>"},{"location":"using-minder/#5-start-it","title":"5. Start it","text":"<pre><code>./minder bootstrap        # look at this machine, say what it needs, write .env\n./minder up               # build, start, and wait until Minder can answer\n</code></pre> <p><code>bootstrap</code> asks before writing anything, and writes only <code>.env</code>. If your machine has a GPU or an AI accelerator it will tell you what else is needed to use it \u2014 and what happens if you skip that, which differs: an NVIDIA card without its toolkit stops the stack from starting at all, while a Coral or Hailo without its driver lets everything start and quietly run on the processor instead.</p> <p>The first run downloads a few GB and takes 10\u201320 minutes. <code>./minder up</code> waits for it and tells you when it is ready. To watch:</p> <pre><code>./minder logs minder\n</code></pre> <p>It is ready when you see <code>Application startup complete</code> \u2014 though <code>./minder up</code> has already waited for that, so you only need the logs if something looks wrong.</p> <p>Check what it is running on:</p> <pre><code>./minder status\n</code></pre> <p>This is the command to remember. It shows which services are up, which processor is doing the vision work, and anything that is working but not the way you configured it. The line to look for:</p> <pre><code>!! MISMATCH   host sees 'cuda', container sees 'generic-cpu'\n              the accelerator is not reaching Minder \u2014 it is running on the CPU\n</code></pre> <p>That means you have hardware Minder cannot reach \u2014 everything works, just slowly, and nothing else would have told you.</p>"},{"location":"using-minder/#6-set-it-up","title":"6. Set it up","text":"<p>Open the web interface in a browser on the same network:</p> <pre><code>hostname -I | awk '{print $1}'    # e.g. 192.168.0.42 \u2192 open http://192.168.0.42\n</code></pre> <p>Use the IP address. You will see <code>http://minder.local</code> in older notes; that only resolves if the host is actually named <code>minder</code> and is running an mDNS responder (<code>avahi-daemon</code> on Linux, Bonjour on macOS). Neither is installed by Minder, so on a fresh box the name will not work and the IP always will. If you want the friendly name:</p> <pre><code>sudo hostnamectl set-hostname minder\nsudo apt install -y avahi-daemon\n</code></pre> <p>Then:</p> <ol> <li>Add cameras. Minder scans your network and lists what it finds. Confirm the ones you want and    give each a name you would actually say out loud \u2014 \"Main Door\", \"Pathway Gate\", \"Driveway\". Those    names are what you use when talking to it.</li> <li>Connect a messaging channel. Telegram or Discord. Scan the QR code, and the bot will message    you to confirm.</li> <li>Connect Home Assistant if you have smart devices. Minder discovers them; you pick which it    may control.</li> </ol> <p>That is the whole setup. Everything else is conversation.</p>"},{"location":"using-minder/#keeping-it-running","title":"Keeping it running","text":"<pre><code>./minder status                    # what's running, and anything not right\n./minder logs minder               # what it's doing\n./minder restart                   # if something seems stuck\n./minder down                      # stop everything (your data is kept)\n./minder up                        # start again\n</code></pre> <p><code>./minder status</code> is the one to reach for first. As well as which services are up, it reports which processor is doing the vision work \u2014 and says so plainly when an accelerator you have is not being used, which otherwise looks exactly like not having one.</p> <p>Minder backs itself up nightly to <code>~/minder/backups</code>. To update:</p> <pre><code>cd ~/minder &amp;&amp; git pull &amp;&amp; ./minder up\n</code></pre>"},{"location":"using-minder/#talking-to-it","title":"Talking to it","text":"<p>Just write normally. Minder works out what you meant.</p> <p>Ask what it sees</p> <p>\"is someone at the main door\" \"do you see anyone outside?\" \"is the gate open?\" \"is there a car in the driveway\" \"show me the porch\"</p> <p>It looks at the camera now, describes what is actually in frame, and sends the snapshot.</p> <p>Control things</p> <p>\"turn off the pressure pump\" \"switch on the solar heater\"</p> <p>Set up rules \u2014 this is the part people underuse:</p> <p>\"turn on the pump when there is a person at the main door\" \"every night at 9pm turn on the porch light\" \"switch off the pump when the water level is reached\"</p> <p>Minder writes the rule, shows it back to you, and runs it from then on. You can list and remove rules the same way.</p> <p>Ask about the house</p> <p>\"what cameras do I have\" \"what smart devices are connected\" \"is it raining outside?\" \"anything at the door today?\"</p>"},{"location":"using-minder/#if-it-misunderstands-you","title":"If it misunderstands you","text":"<p>It gets the intent right about 19 times in 20 \u2014 that is measured, not a guess, across 166 real phrasings (<code>scripts/bench_router.py</code> if you want to run it yourself). The twentieth time, the most useful thing you can do is reply <code>wrong</code>. Minder records the message it misread, asks what you meant, and <code>wrong: query</code> (or <code>snapshot</code>, <code>device_now</code>, <code>scenario</code>, \u2026) files it as a test case that the next version is checked against. Beyond that, three things help:</p> <ul> <li>Name the camera. \"anything on bedroom deck\" is more reliable than \"anything out back\", because   Minder matches your configured camera names exactly. Names it does not know (\"the veranda\") are   where it struggles most.</li> <li>Say what you want done. \"turn the pump off\" rather than \"the pump\" \u2014 a command without an   action reads as a question about your devices.</li> <li>Say when for a rule. \"turn on the pump when someone is at the door\" creates a standing   rule; without a when/if/every, the same words are treated as \"do it now\".</li> </ul> <p>Every message you send is recorded with what Minder thought you meant, so misreadings can be found and fixed rather than guessed at. Set <code>MINDER_ROUTER_LOG=off</code> if you would rather it did not.</p>"},{"location":"using-minder/#knowing-who-is-who-optional","title":"Knowing who is who (optional)","text":"<p>Minder can put names to faces \u2014 but only for people you deliberately enrol, and only if you install the face plugins. Nothing about faces is stored or computed until you do.</p> <p>The most useful thing it does needs no enrolment at all: telling you that someone is a stranger. A stranger is a face that matched nobody, so \"alert me if an unrecognised face is at the gate after dark\" works for people who have never been near your house.</p> <p>If you also enrol the people who live and work there, alerts can say \"Alex at the main door\" instead of \"person at the main door\", and you can write rules about groups \u2014 \"anyone who is not family, after 10pm\".</p> <p>A recognised face never silences an alert. It changes the words, never whether you are told. So if it misidentifies someone, you get a wrong name \u2014 never a missed intruder.</p> <p>Setup, groups, rules, and how to delete someone: Face detection and recognition.</p>"},{"location":"using-minder/#alerts","title":"Alerts","text":"<p>When a camera detects something worth telling you about, Minder sends a message with a snapshot and a short description of what it is \u2014 not just \"motion detected\", but what is actually there.</p> <p>If you get too many alerts, say so: \"stop alerting me about cars in the driveway\", or set quiet hours. Alerting rules are rules like any other.</p> <p>If you are missing alerts, check the camera is armed in the web UI. Minder only alerts on cameras you have armed.</p>"},{"location":"using-minder/#asking-what-happened","title":"Asking what happened","text":"<p>\"what happened today between 11 and 3\" \"did anyone come to the main door this afternoon\" \"was there a car at the porch yesterday\" \"who was at the door this morning\" / \"did anyone unknown come today\" \"what did you not tell me about last night\"</p> <p>Minder answers from its own record of every camera event \u2014 the ones it alerted you about and the ones it did not (no rule, cooldown, or the second look disagreed), each marked as such \u2014 with the times in your clock and the top events with their photos. If a camera was offline for part of the window it says so, because \"nothing happened\" is only true if Minder was looking. The dashboard's Events tab has the same query as a form.</p>"},{"location":"using-minder/#snapshots-video-and-history","title":"Snapshots, video and history","text":"<p>Every alert is kept. In the web UI you can scroll back through events, view the snapshot or the clip, and mark things as seen. Ask in chat too: \"anything at the gate yesterday?\"</p>"},{"location":"using-minder/#when-minder-asks-you-something","title":"When Minder asks you something","text":"<p>Minder runs a health check every six hours. If it finds a problem it can fix \u2014 a camera that dropped off, a service that needs restarting \u2014 it asks you first and waits.</p> <p>It never repairs anything on its own. Reply to approve, or ignore it and nothing happens.</p>"},{"location":"using-minder/#privacy","title":"Privacy","text":"<p>By default nothing leaves your house. Camera images are analysed by a model running on the appliance itself.</p> <p>Cloud AI is opt-in and used only when you turn it on \u2014 it is better at detailed scene descriptions, and it is what powers one-time training when you teach Minder to recognise something new. When it is off, Minder still works; descriptions are just less detailed.</p> <p>Your recordings are never uploaded. Snapshots go to a cloud model only if you enabled cloud vision, and only for the specific question being asked.</p>"},{"location":"using-minder/#troubleshooting","title":"Troubleshooting","text":"What you see What to do Bot does not reply Check the appliance is on \u2014 the web interface at its IP should load. \"I can't reach that camera\" The camera is offline or changed IP. Re-run discovery in the web UI. Alerts stopped Check the camera is still armed, and that quiet hours are not on. Device won't switch Home Assistant may have lost the device. Ask \"diagnose\" and Minder will check. Everything seems stuck Ask \"diagnose\". Minder checks itself and reports what is wrong. <p>If you need to start clean, the web UI has a backup and restore page. Minder backs itself up nightly, so restoring is usually faster than reconfiguring.</p>"},{"location":"using-minder/#where-the-appliance-lives","title":"Where the appliance lives","text":"<p>Minder runs from <code>~/minder</code> on the host, as a set of Docker containers. Day to day you should never need to touch that \u2014 but if you want the technical picture, see the architecture guide.</p>"},{"location":"walkthrough/","title":"Walkthrough","text":"<p>Six short clips of the web portal, with no narration \u2014 each is one tab, doing the thing that tab is for.</p> <p>Everything on screen is invented</p> <p>These were recorded against a seeded demo instance, not a working appliance. The cameras (<code>Front-Door</code>, <code>Driveway</code>, <code>Garden</code>, <code>Side-Gate</code>) do not exist, the events never happened, and every still is generated \u2014 each one carries a <code>SYNTHETIC DEMO FRAME</code> label in its corner so you never have to wonder whether you are looking at somebody's front door.</p> <p>That is the trade: the frames look synthetic because they are. A demo of a camera system is a poor reason to publish footage of a real house, and a product that argues for careful handling of other people's faces should not open by putting a household on the internet.</p> <p>The seeder is <code>scripts/seed_demo.py</code>; the recorder is <code>scripts/record_demo.mjs</code>, and it refuses to run against anything but the seeded instance.</p>"},{"location":"walkthrough/#the-whole-portal","title":"The whole portal","text":"<p>Every tab in one pass \u2014 start here if you only watch one.</p>"},{"location":"walkthrough/#overview","title":"Overview","text":"<p>Cameras online, unread events, whether Home Assistant is connected, how many rules are active, and recent activity.</p>"},{"location":"walkthrough/#events","title":"Events","text":"<p>What each camera saw, described in a sentence rather than logged as \"motion\". Unread events are marked; the thumbnail is the frame the description was written from.</p>"},{"location":"walkthrough/#cameras","title":"Cameras","text":"<p>The inventory, with a thumbnail and a live view per camera, and network discovery for adding more.</p>"},{"location":"walkthrough/#rules","title":"Rules","text":"<p>Standing rules \u2014 what to watch for, on which cameras, between which hours. New ones can be written in plain language; the model parses the sentence into a rule you can then read and edit.</p>"},{"location":"walkthrough/#people","title":"People","text":"<p>Face enrolment, and the consent text that goes with it. Worth pausing on: it states what is stored, what is discarded, and that a recognised face never silences an alert.</p> <p>The clip shows the tab with nobody enrolled \u2014 which is the state that matters most, because it is the one where every face is reported as unrecognised.</p> <p>What is actually stored \u2192</p>"}]}